# Aminyo OS API

Backend API for Aminyo OS agent system.

## Endpoints

- `GET /health` — Health check
- `GET /api/status` — System status dashboard
- `POST /api/webhook/:agent` — Inter-agent messages

## Deploy

Auto-deployed on Railway from `main` branch.
