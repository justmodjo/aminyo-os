## 2026-06-02 - Correction flux lead officiel (commit c94d5f5)

**Flux corrigé :** Formulaire → Netlify → OpenClaw → Supabase leads + Telegram

**Changements clés :**
- Header Authorization corrigé : `'Bearer ' + token` (était `'***' + token` — le `***` cassait l'auth)
- Colonnes strictes : `first_name, last_name, email, phone, budget_range, project_type, project_description, source` — sans les colonnes superflues (company, ai_score, pipeline_stage, etc.)
- Ordre : Telegram en premier (garantit la notification même si Supabase est down), Supabase en second
- Make retiré du flux (comme demandé par Amine)
- Réponse JSON : `{ ok, telegram, crm, runId, message }` avec statuts individuels

**Pushé sur justmodjo/aminyo-agents** (netlify-webhook-relay.js)
